# =============================================================
# PitSimulator — HAL LED
# El LED no necesita lógica adicional:
# Pin.on() / Pin.off() del HAL base ya emiten GPIO:N:V.
# Este archivo existe solo como marcador para futuros
# LEDs con comportamiento especial (ej: PWM, RGB).
# =============================================================